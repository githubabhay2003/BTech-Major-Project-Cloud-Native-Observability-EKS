name: Deploy to EKS

on:
  push:
    branches:
      - main
      - refactor/terraform-modules

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
